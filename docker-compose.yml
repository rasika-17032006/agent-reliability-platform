version: '3.8'

services:
  gitea:
    image: gitea/gitea:latest
    container_name: agent_gitea
    ports:
      - "3000:3000"
    environment:
      - USER_UID=1000
      - USER_GID=1000
      - GITEA__database__DB_TYPE=sqlite3
      - GITEA__server__ROOT_URL=http://localhost:3000/
      - GITEA__server__HTTP_PORT=3000
      - GITEA__service__DISABLE_REGISTRATION=false
    volumes:
      - gitea_data:/data

volumes:
  gitea_data: